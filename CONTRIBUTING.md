# Contributing

1. Create an issue first (feature/bug).
2. Branch from `main`: `git checkout -b feat/<short-name>`
3. Keep notebooks small; move reusable code to `src/`.
4. Run `make fmt lint test` before PR.
5. Open a Pull Request and request 1 review.
